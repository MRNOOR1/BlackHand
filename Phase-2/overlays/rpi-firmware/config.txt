# Black Hand OS - Raspberry Pi 5 Boot Config

# Enable I2C for HyperPixel 4 touch controller
dtparam=i2c_arm=on
dtparam=i2c1=on

# Enable SPI (useful for some peripherals)
dtparam=spi=on

# Enable audio
dtparam=audio=on

# HyperPixel 4 display overlay (uncomment when ready)
# dtoverlay=hyperpixel4

# GPU memory allocation
gpu_mem=128

# Enable 64-bit mode
arm_64bit=1
